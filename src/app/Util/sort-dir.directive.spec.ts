import { SortDirDirective } from './sort-dir.directive';

describe('SortDirDirective', () => {
  it('should create an instance', () => {
    const directive = new SortDirDirective();
    expect(directive).toBeTruthy();
  });
});
